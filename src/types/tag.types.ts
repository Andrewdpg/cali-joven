/**
 * Tipo `Tag`
 *
 * Representa una etiqueta utilizada para categorizar publicaciones, eventos u otros recursos dentro del sistema.
 *
 * @property {string} name - Nombre de la etiqueta (ejemplo: "Tecnología", "Educación").
 * @property {string} description - Descripción breve de la etiqueta y su propósito.
 */
export type Tag = {
  name: string;
  description: string;
};
